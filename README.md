# Setup

Install dependencies
```bash
# with yarn
yarn install

# with npm
npm install
```

# Run locally

```bash
# with yarn
yarn dev

# with npm
npm run dev
```

The web app should be available via
http://localhost:3000
